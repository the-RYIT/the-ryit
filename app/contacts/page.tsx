import { DetailSection } from "@/components/contact/DetailSection";

const page = () => {
  return (
    <main className="w-screen mainSection">
      <DetailSection/>
    </main>
  );
};

export default page;
